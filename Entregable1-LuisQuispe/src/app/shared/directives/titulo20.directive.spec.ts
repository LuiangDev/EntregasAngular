import { Titulo20Directive } from './titulo20.directive';

describe('Titulo20Directive', () => {
  it('should create an instance', () => {
    const directive = new Titulo20Directive();
    expect(directive).toBeTruthy();
  });
});
